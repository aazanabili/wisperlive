# WhisperLive 🎙️✨

تطبيق ويندوز خفيف جداً يتيح لك "الكتابة الصوتية الذكية المباشرة" (Voice-to-Text Auto-Typing) في أي مكان تقف فيه بمؤشر الفأرة (متصفح، وورد، محادثات)، معتمداً على قوة ونماذج **Google Gemini Flash** للترجمة والتصحيح وإعادة الصياغة.

## ✨ المميزات
- **تسجيل في الخلفية:** التطبيق يعمل في الخلفية بهدوء.
- **اختصارات سريعة (Global Hotkeys):** تحكم بالتسجيل بضغطة زر واحدة (الافتراضي: `Ctrl + Space`).
- **اللصق التلقائي:** بمجرد الانتهاء من الحديث، سيفهم الذكاء الاصطناعي ما قلته، يصحح الأخطاء، يترجمه للغتك المطلوبة، ثم يطبع النص تلقائياً `Ctrl+V` في نافذتك المفتوحة.
- **إعدادات مرنة:** اختر لغة الخرج (عربي، إنجليزي، الخ)، وحدد اختصارك المفضل.
- **مؤشر تسجيل عائم:** تظهر حالة واضحة فوق التطبيقات أثناء التسجيل والمعالجة.
- **تكامل مع شريط النظام:** يمكن تصغير التطبيق بجانب الساعة، مع خيارات البدء مصغراً والتشغيل التلقائي عند تسجيل الدخول إلى ويندوز.
- **تحديث مدمج:** زر **Update** يفحص GitHub ويثبت آخر إصدار تلقائياً.
- **اختصار قابل للتسجيل:** انقر حقل الاختصار أو زر **Record** ثم اضغط المفاتيح المطلوبة.

---

## 🚀 كيفية التحميل للمستخدمين (للزوار)
لا تحتاج إلى تثبيت أي لغات برمجة، التطبيق جاهز ومستضاف هنا عبر نظام CI/CD (البناء التلقائي).
1. اذهب إلى قسم **[Releases](../../releases/latest)** في يمين صفحة هذا المستودع.
2. قم بتحميل الملف التنفيذي **`WhisperLive.exe`**.
3. ضع البرنامج في أي مجلد تريده (مثلاً: مجلد جديد على سطح المكتب).

## 🔑 خطوة إلزامية قبل التشغيل (تجهيز مفتاح API)
التطبيق يحتاج إلى مفتاح اتصال رسمي ومجاني ليتواصل مع ذكاء جوجل.
1. اذهب إلى [Google AI Studio](https://aistudio.google.com/app/apikey).
2. قم بإنشاء مفتاح جديد (Create API Key) - (تأكد أن المفتاح يبدأ بـ `AIzaSy...`).
3. في نفس المجلد الذي وضعت فيه `WhisperLive.exe`، قم بإنشاء ملف نصي جديد وسمّه بالضبط **`.env`** (تأكد من عدم وجود امتداد .txt).
4. افتح الملف بالمفكرة (Notepad) واكتب بداخله:
   ```
   GEMINI_API_KEY=ضع_مفتاحك_هنا
   ```
5. احفظ الملف، والآن يمكنك تشغيل البرنامج بضغطة مزدوجة! 

---

## 🛠️ كيفية الاستخدام
1. بعد تشغيل البرنامج، ستظهر نافذة الإعدادات. فعّل خيار **Minimize to the system tray** لإخفائها بجانب الساعة بدلاً من شريط المهام. انقر بزر الفأرة الأيمن على أيقونة WhisperLive في شريط النظام لاستعادة النافذة أو إنهاء التطبيق.
2. ضع المؤشر في مكان تريد الكتابة فيه.
3. اضغط الاختصار (`Ctrl+Space`).
4. ستسمع **تنبيه صوتي (Beep)**، ابدأ بالحديث.
5. اضغط الاختصار مرة أخرى لإنهاء التسجيل، ستسمع صوت توقف.
6. انتظر ثوانٍ معدودة، ستسمع **صوت نجاح (Success Beep)** وسيتم طبع النص منسقاً واحترافياً.
7. استخدم زر **Update** في نافذة الإعدادات للحصول على آخر إصدار من GitHub. عند تشغيل التطبيق من ملف المصدر، يتم استخدام `git pull --ff-only` للحفاظ على التغييرات المحلية بأمان.

---

## ⚙️ إعداد نظام البناء التلقائي (CI/CD) - للمطور
هذا المستودع مزود بنظام GitHub Actions ليقوم ببناء ملف الـ `.exe` تلقائياً.

### كيف تصدر نسخة جديدة للمستخدمين؟
عند إجراء أي تعديلات على الكود (مثل `main.py` أو غيره) وترغب في جعل البرنامج المحدث متاحاً للتحميل للعموم:
1. ارفع الكود الجديد إلى المستودع (Push).
2. قم بإنشاء **Tag** جديد يبدأ بحرف `v` (مثال: `v1.0.0` أو `v1.1`).
   يمكنك فعل ذلك من خلال سطر الأوامر:
   ```bash
   git tag v1.0.0
   git push origin v1.0.0
   ```
3. أو يمكنك الذهاب إلى صفحة (Releases) في جيتهاب، والضغط على **Draft a new release** وتسمية الـ Tag بـ `v1.0.0` ثم نشرها.
4. **سيبدأ النظام تلقائياً** في الخلفية، سيقوم بتجهيز بيئة Windows، تحميل المكتبات، تحويل كود بايثون إلى `WhisperLive.exe`، ثم إرفاق الملف كأصل (Asset) جاهز للتحميل في صفحة الـ Releases ليراه جميع الزوار.

### البناء اليدوي (بدون إصدار Release)
إذا أردت فقط بناء ملف لتختبره أنت من خلال جيتهاب:
1. اذهب إلى تبويب **Actions**.
2. اختر **Build and Release EXE** من القائمة الجانبية.
3. اضغط على زر **Run workflow**.
4. بعد الانتهاء، ستجد ملف الـ `exe` بانتظارك في أسفل صفحة التقرير في قسم **Artifacts** باسم `WhisperLive-Windows`.
